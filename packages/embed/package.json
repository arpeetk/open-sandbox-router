{
  "name": "@osr/embed",
  "version": "0.1.0",
  "type": "module",
  "description": "Embedded OSR runtime: assemble the registry, credentials, and service from env — shared by the gateway and the CLI's local mode.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@osr/core": "workspace:*",
    "@osr/adapter-sim": "workspace:*",
    "@osr/adapter-e2b": "workspace:*",
    "@osr/adapter-modal": "workspace:*",
    "@osr/adapter-vercel": "workspace:*",
    "@osr/adapter-kubernetes": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
