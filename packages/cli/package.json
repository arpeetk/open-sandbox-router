{
  "name": "@osr/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "Command-line interface for Open Sandbox Router.",
  "license": "Apache-2.0",
  "bin": {
    "osr": "./bin/osr.mjs"
  },
  "files": [
    "bin"
  ],
  "dependencies": {
    "@osr/core": "workspace:*",
    "@osr/sdk": "workspace:*",
    "@osr/embed": "workspace:*"
  },
  "optionalDependencies": {
    "modal": ">=0.9.0",
    "@vercel/sandbox": ">=2.0.0",
    "@kubernetes/client-node": ">=0.21.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "prepublishOnly": "node build.mjs",
    "typecheck": "tsc --noEmit"
  }
}
