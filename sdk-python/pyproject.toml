[project]
name = "osr-sdk"
version = "0.1.0"
description = "Python client SDK for Open Sandbox Router"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["osr"]
